//objects

/*const person = {
    name:'John',
    age: 30,
    weight: 88.6,
    isAdmin: true

}
*/

const person = { 
    nome: 'Douglas',
    idade: 27,
    especialidade: 'Macroeconomia',
    escola: 'heterodoxa, keynesiana/estruturalista'

}

console.log(`${person.nome} tem ${person.idade} anos e seu campo de pesquisa é em ${person.especialidade} de formação é ${person.escola}`)

//console.log(`${person.name} tem ${person.age} anos e ${person.weight} quilos`)
//console.log(`${person.name}, tem ${person.age} anos e ${person.weight} quilos`)